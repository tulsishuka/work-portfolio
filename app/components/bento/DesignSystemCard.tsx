import React from "react";
import { X, Menu, Check } from "lucide-react";
import Image from "next/image";

const DesignSystemCard = () => {
  return (
    <div className="md:col-span-2 md:row-span-2 bg-[#0d1525] rounded-[32px] p-8 relative overflow-hidden border border-slate-800/50">
      {/* content same as your first big card */}
    </div>
  );
};

export default DesignSystemCard;
